{
  "name": "guys.wtf",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "deploy": "yarn build && push-dir --dir=out --branch=gh-pages"
  },
  "dependencies": {
    "next": "^10.0.1",
    "push-dir": "^0.4.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@types/node": "^14.0.5",
    "@types/react": "^16.9.35",
    "typescript": "^4.0.5"
  }
}
